# Svelte Todo App

Minimal Svelte todo uygulaması — ekleme, silme, düzenleme ve tamamlandı işaretleme.

Kurulum:

```bash
cd svelte/svelte-todo-app
npm install
npm run dev
```

Uygulama `localStorage` ile kalıcı hale getirildi.
